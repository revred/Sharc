@* Page 8: CTA - conversion paths with benchmark proof *@

<div class="cta-page">
    <div class="cta-badge">ADOPTION PATH</div>
    <h2 class="cta-title">From Arena Proof to<br />Production Rollout</h2>
    <p class="cta-subtitle">
        Sharc is measured against veteran engines and tuned for modern AI runtime constraints.
        Pick the path that matches your adoption stage.
    </p>

    <div class="cta-proof-strip">
        <div class="cta-proof-item">
            <span class="cta-proof-label">Sharc wins vs SQLite</span>
            <span class="cta-proof-value">@SharcWinsVsSqlite/@ComparableSlidesVsSqlite</span>
        </div>
        <div class="cta-proof-item">
            <span class="cta-proof-label">Median speedup</span>
            <span class="cta-proof-value">@FormatRatio(MedianSpeedupVsSqlite)x</span>
        </div>
        <div class="cta-proof-item">
            <span class="cta-proof-label">Best speedup</span>
            <span class="cta-proof-value">@FormatRatio(BestSpeedupVsSqlite)x</span>
        </div>
        <div class="cta-proof-item">
            <span class="cta-proof-label">Cold start advantage</span>
            <span class="cta-proof-value">@FormatRatio(ColdStartSpeedup)x</span>
        </div>
    </div>

    <div class="cta-paths">
        <div class="cta-card developer">
            <span class="cta-card-icon">&#x1F4BB;</span>
            <h3 class="cta-card-title">Developer</h3>
            <p class="cta-card-desc">Drop-in package, zero native dependencies, benchmark-ready setup in minutes.</p>
            <div class="cta-card-actions">
                <code class="cta-nuget">dotnet add package Sharc</code>
                <a href="https://github.com/revred/Sharc" target="_blank" rel="noopener noreferrer" class="btn-primary cta-btn">Start on GitHub</a>
            </div>
        </div>

        <div class="cta-card decision-maker">
            <span class="cta-card-icon">&#x1F4CA;</span>
            <h3 class="cta-card-title">Decision Maker</h3>
            <p class="cta-card-desc">Use Arena evidence to justify lower latency, smaller footprint, and built-in trust controls.</p>
            <div class="cta-card-actions">
                <a href="https://www.linkedin.com/in/revodoc/" target="_blank" rel="noopener noreferrer" class="btn-secondary cta-btn">Discuss Adoption</a>
            </div>
        </div>

        <div class="cta-card enterprise">
            <span class="cta-card-icon">&#x1F3E2;</span>
            <h3 class="cta-card-title">Enterprise</h3>
            <p class="cta-card-desc">Plan migration from legacy embedded stacks with workload profiling and rollout guidance.</p>
            <div class="cta-card-actions">
                <a href="https://www.linkedin.com/in/revodoc/" target="_blank" rel="noopener noreferrer" class="btn-secondary cta-btn">Contact Team</a>
            </div>
        </div>
    </div>

    <div class="cta-footer">
        MIT License &middot; Pure .NET 10 &middot; Arena-verified benchmark narrative
    </div>
</div>

@code {
    [Parameter] public int SharcWinsVsSqlite { get; set; }
    [Parameter] public int ComparableSlidesVsSqlite { get; set; }
    [Parameter] public double MedianSpeedupVsSqlite { get; set; } = 1.0;
    [Parameter] public double BestSpeedupVsSqlite { get; set; } = 1.0;
    [Parameter] public double ColdStartSpeedup { get; set; } = 1.0;

    private static string FormatRatio(double value)
    {
        if (value < 1.0) return "1.0";
        if (value >= 100) return value.ToString("F0");
        if (value >= 10) return value.ToString("F1");
        return value.ToString("F2");
    }
}
